name: toy_pkg
channels:
    - conda-forge
dependencies:
    - pip
    - numpy
    - scipy
    - matplotlib==3.3.2
    - pandas
    - scikit-learn
    - jupyter
    - joblib
    - pytest==6.1.0
    - pytest-cov
